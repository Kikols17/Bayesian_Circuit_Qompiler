experiment:
  name: "random_bn_dcm"
  output_dir: "outputs/random_bn_dcm"
  output:
    save_cpd_md: true
    save_circuit_image: true

network:
  builder: "generator"
  save_image: true
  seed: 17
  type: "random"
  params:
    num_nodes: 5
    max_parents: 2
    max_states: 3

inference:
  compiler: "DCM"
  encoding: "binary"
  baseline:
    enabled: true
    method: "variable_elimination"
    params: {}
  circuit:
    shots: 1024
    seed: 123
  backend:
    type: "pennylane.simulator"
    device: "default.qubit"
    wires: null
  evidence: {}
  query:
    - "X0"
